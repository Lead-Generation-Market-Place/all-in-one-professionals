import 'package:flutter/material.dart';

class ListsTab extends StatelessWidget {
  const ListsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Lists Tab', style: Theme.of(context).textTheme.bodyMedium),
    );
  }
}
